page {
    background: #f7f8fa;
    height: 100vh;
}

.view-body {
    height: calc(100vh - 44px);
    overflow: auto;
}

.btn-list,.view-body {
    position: absolute;
    width: 100%;
}

.btn-list {
    bottom: 0;
}

.mask {
    height: 100vh;
    position: absolute;
    top: 0;
    width: 100%;
    z-index: 99;
}

textarea {
    background: #fff;
    height: 300rpx;
    padding: 0 16px 10px;
    width: calc(100% - 32px);
}

.list-body {
    left: 5%;
    position: relative;
    width: 90%;
}

.list-body>.card {
    background: #fff;
    border-radius: 32rpx;
    box-shadow: 0 0 20rpx hsla(0,0%,71%,.4);
    margin-bottom: 32rpx;
    position: relative;
    width: 100%;
}

.card:last-child {
    margin-bottom: 0;
}

.card>.title {
    padding: 32rpx 32rpx 12rpx;
}

.card>.card-num {
    padding: 0 32rpx 8rpx;
}

.card>.card-num,.card>.phone-num {
    color: #aaa;
    font-size: .8rem;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
}

.card>.phone-num {
    padding: 0 32rpx 32rpx;
}

.card>.close-icon {
    position: absolute;
    right: 32rpx;
    top: 32rpx;
}

.close-icon:active {
    opacity: .8;
}

.add-new {
    color: #888;
    height: 120rpx;
    line-height: 120rpx;
    text-align: center;
}

.add-new:active {
    background-color: #fafafa;
}