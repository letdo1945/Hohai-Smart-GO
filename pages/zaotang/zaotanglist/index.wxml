<view class="list-body">
    <van-cell isLink bindtap="onSelect" data-item="{{item}}" title="{{item.name}}-{{item.type=='1'?'男':'女'}}" value="{{item.campusName}}" wx:for="{{list}}" wx:key="index"></van-cell>
</view>
